{
  "name": "neon-channel-vr",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assemble": "node scripts/assemble.mjs",
    "dev": "npm run assemble && vite",
    "build": "npm run assemble && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.185.0"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
